def doSomething(text: str, bool2: bool, num: number, list2: List[any], sprite: game.LedSprite, image: Image):
    return 0
input.set_sound_threshold(SoundThreshold.LOUD, input.rotation(Rotation.PITCH))
led.toggle(0, 0)
radio.set_transmit_serial_number(False)

def on_forever():
    pass
basic.forever(on_forever)
