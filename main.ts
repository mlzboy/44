function doSomething (text: string, bool: boolean, num: number, list: any[], sprite: game.LedSprite, image: Image) {
    return 0
}
input.setSoundThreshold(SoundThreshold.Loud, input.rotation(Rotation.Pitch))
led.toggle(0, 0)
radio.setTransmitSerialNumber(false)
basic.forever(function () {
	
})
